name: Release (Preview)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/release-template.yml
    with:
      version: 0.4.1-preview.${{ github.run_number }}
    secrets: inherit