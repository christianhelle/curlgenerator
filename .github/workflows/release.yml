name: Release

on:
  workflow_dispatch:
  push:
    branches:
      - "release"

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/release-template.yml
    with:
      version: 0.4.1
      publish-crates: true
    secrets: inherit